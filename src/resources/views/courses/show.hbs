<div class="mt-4 mb-4">
    <div class="row">
        <div class="col-lg-3">
            <button>Học Ngay</button>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <iframe width="560" height="315" src="https://www.youtube.com/embed/{{course.videoId}}"></iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>